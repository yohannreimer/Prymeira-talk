{
  "name": "prymeira-talk",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm --parallel dev",
    "demo:prymeira": "PRYMEIRA_LOCAL_AUTH_BYPASS=true PRYMEIRA_LOCAL_WORKSPACE_ID=demo_workspace PRYMEIRA_LOCAL_ROLE=owner VITE_LOCAL_AUTH_BYPASS=true VITE_API_URL=http://localhost:3002 pnpm --parallel dev",
    "demo:reset": "docker compose -f docker-compose.dev.yml up -d && pnpm prisma:generate && pnpm --filter @prymeira-talk/api prisma db push && pnpm --filter @prymeira-talk/api seed:demo",
    "dev:api": "pnpm --filter @prymeira-talk/api dev",
    "dev:web": "pnpm --filter @prymeira-talk/web dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "prisma:generate": "pnpm --filter @prymeira-talk/api prisma generate",
    "prisma:migrate": "pnpm --filter @prymeira-talk/api prisma migrate dev"
  },
  "devDependencies": {
    "@types/node": "^22.16.5",
    "typescript": "^5.8.3"
  },
  "pnpm": {
    "overrides": {
      "dompurify": "^3.4.11",
      "ws": "^8.21.0"
    }
  },
  "packageManager": "pnpm@10.0.0"
}
